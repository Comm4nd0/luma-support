# Luma Support Mobile

Flutter app for Luma Tech engineers and clients — one codebase, role-adaptive UI.

## What's in here

| Area | Status |
|------|--------|
| Auth (JWT login + refresh interceptor) | ✅ |
| Typed models + repositories for every API resource | ✅ |
| `go_router` routing with auth redirect + deep links | ✅ |
| Engineer + Client shells (bottom-nav) | ✅ |
| Dashboards, tickets, KB, notifications inbox, client detail, profile | ✅ |
| Push notifications (FCM token register, foreground render, deep-link tap) | ✅ |
| Crashlytics for uncaught errors | ✅ |
| Unit + widget tests | ✅ |
| GitHub Actions CI (analyze + tests; build AAB/IPA on tag) | ✅ |
| `ios/` + `android/` platform folders | ⛔ generated locally |
| Firebase config files (`google-services.json`, `GoogleService-Info.plist`) | ⛔ generated by `flutterfire configure` |
| App icons + splash source PNGs | ⛔ Marco's brand assets |

## Bootstrap (one-time)

```bash
cd mobile
flutter create --platforms=ios,android --org com.lumatechsolutions \
  --project-name luma_support_mobile .
flutter pub get
```

Then drop in the brand source assets per `assets/README.md` and generate:

```bash
dart run flutter_launcher_icons
dart run flutter_native_splash:create
```

## Develop

```bash
flutter run \
  --dart-define=API_BASE=http://10.0.2.2:8006/api/v1   # Android emulator
```

For a physical device, point `API_BASE` at your LAN IP, e.g.
`http://192.168.1.10:8006/api/v1`.

## Test

```bash
flutter analyze
flutter test
```

The test suite covers:

- `TicketsRepository`, `NotificationsRepository`, `KnowledgeRepository`
  unit tests with a mocked Dio (`mocktail`).
- `AuthInterceptor` integration tests with scripted `HttpClientAdapter`s:
  Bearer header attach, 401→refresh→retry, max-one retry, refresh-fail
  logs the user out.
- Widget tests for `LoginScreen`, `TicketListScreen` (render / empty /
  error), and `TicketDetailScreen` role visibility (engineer vs client).

## Push notifications

`PushService` is fully wired. Before push will reach a device:

1. `dart pub global activate flutterfire_cli`.
2. `flutterfire configure` from this directory.
3. Drop `google-services.json` into `android/app/` and
   `GoogleService-Info.plist` into `ios/Runner/` (gitignored — never commit).
4. Upload the APNs `.p8` key in the Firebase console
   (Cloud Messaging → iOS app config).
5. On the backend, set `FCM_ENABLED=True` and `FIREBASE_CREDENTIALS_JSON`
   to the path of the Firebase Admin service-account JSON.

Once those are in place every `Notification` row triggers
`notifications.tasks.send_push` via the `post_save` signal; the device
receives a push that deep-links to `/tickets/<id>` via `PushRouter`.

## Release

CI (`.github/workflows/mobile.yml`) builds an AAB on `ubuntu-latest`
and an IPA on `macos-14`, both gated on a `v*` tag. Signing requires
these secrets in the repo:

| Secret | Purpose |
|--------|---------|
| `ANDROID_KEYSTORE_BASE64` | base64-encoded upload keystore |
| `ANDROID_KEYSTORE_PASSWORD` | keystore password |
| `ANDROID_KEY_ALIAS` | key alias inside the keystore |
| `ANDROID_KEY_PASSWORD` | key password |
| `MATCH_PASSWORD` | passphrase for `fastlane match` |
| `MATCH_GIT_URL` | private repo hosting certificates |
| `APP_STORE_CONNECT_API_KEY` | JSON for App Store Connect API key |
| `PROD_API_BASE` | e.g. `https://api.lumatechsolutions.co.uk/api/v1` |

## Outstanding Marco-side prerequisites

These gate the public release, not development. Start them in parallel.

- [ ] Apple Developer Program enrolment (company name).
- [ ] Google Play Console enrolment (company name).
- [ ] Firebase project — recommend separate dev/prod.
- [ ] APNs `.p8` key uploaded to Firebase.
- [ ] Privacy policy URL hosted (data collected: email, ticket content,
      optional photos, FCM device tokens).
- [ ] Brand source assets at `assets/icon/*.png` and `assets/splash/*.png`.
- [ ] Confirm bundle ID `com.lumatechsolutions.support` is free on both stores.
